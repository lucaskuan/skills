# CLAUDE.md

**[AGENTS.md](./AGENTS.md) is the source of truth for this repo's conventions.** Read it first.

## Cross-tool note

Cursor does not read this file. It reads `AGENTS.md` and `.cursor/rules/*.mdc`. Keep conventions in `AGENTS.md` so every tool sees the same rules, and keep this file as a pointer only.
